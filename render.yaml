services:
  # API GATEWAY
  - type: web
    name: jtx-gateway
    env: node
    plan: free
    buildCommand: cd backend/api-gateway && npm install
    startCommand: cd backend/api-gateway && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /health
    autoDeploy: true

  # AUTH SERVICE
  - type: web
    name: jtx-auth
    env: node
    plan: free
    buildCommand: cd backend/auth-service && npm install
    startCommand: cd backend/auth-service && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: JWT_SECRET
        generateValue: true
    autoDeploy: true

  # USERS SERVICE
  - type: web
    name: jtx-users
    env: node
    plan: free
    buildCommand: cd backend/users-service && npm install
    startCommand: cd backend/users-service && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3002
    autoDeploy: true

  # EMPLOYEES SERVICE
  - type: web
    name: jtx-employees
    env: node
    plan: free
    buildCommand: cd backend/employees-service && npm install
    startCommand: cd backend/employees-service && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3003
      - key: MONGODB_URI
        sync: false
    healthCheckPath: /health
    autoDeploy: true
